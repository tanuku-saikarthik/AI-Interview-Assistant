server {
listen 80;
server_name _;
root /usr/share/nginx/html;
index index.html;
# serve static
location / {
try_files $uri $uri/ /index.html;
}


# optional: increase body size for resume uploads
client_max_body_size 10M;
}